export class Disciplina {
    disciplinaId: number;
    nome: string;

    constructor(disciplinaId: number, nome: string) {
        this.disciplinaId = disciplinaId;
        this.nome = nome;
    }
}